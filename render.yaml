services:
  - name: submission-webhook
    type: web
    env: node
    plan: free
    buildCommand: 
    - npm install 
    - cd packages/db/prisma && npm install && npm run postinstall 
    - cd ../../../
    - cd apps/submission-webhook && npm install && npm run build && npm run postbuild
    startCommand: cd apps/submission-webhook && npm start
    branch: main